import 'package:flutter/material.dart';

class DevotionPage extends StatelessWidget {
  const DevotionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Welcome to Devotion Page'));
  }
}
